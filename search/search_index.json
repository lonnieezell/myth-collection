{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Collections","text":"<p>Collections are a powerful, array-like data structure that allow you to work with your data in a more expressive way than traditional arrays. They are especially useful when working with data that is returned from a database, or when working with JSON data. At their heart they are modelled after the Javascript array functionality, and have been expanded from there.</p> <p>Collection instances are immutable. This means that any action that would modify the item array will return a new Collection instance containing the results of the previous action, leaving the original data untouched.</p>"},{"location":"#a-quick-example","title":"A Quick Example","text":"<p>A new collection is created by creating a new instance of <code>Myth\\Collection\\Collection</code>. You can populate the items in the collection at instantation by passing an array into the constructor.</p> <pre><code>use Myth\\Collection\\Collection;\n\n$items = ['a' =&gt; 1, 'b' =&gt; 2, 'c' =&gt; 3, 'd' =&gt; 4];\n$collection = new Collection($items);\n\n$firstEvenItem = $collection\n    -&gt;filter(function($value, $key) {\n        return $value % 2 === 0;\n    })-&gt;first();\n</code></pre> <p>You can integrate most of the following methods into your own work by using the <code>Myth\\Collection\\CollectionTrait</code> within your class.</p> <p>All collections can be accessed via standard array access:</p> <pre><code>$items = ['a' =&gt; 1, 'b' =&gt; 2, 'c' =&gt; 3, 'd' =&gt; 4];\n$collection = new Collection($items);\n\necho $collection['b'];\n</code></pre>"},{"location":"#available-methods","title":"Available Methods","text":"at average count column diff each every filter fill find findIndex first flatten groupBy includes isEmpty indexOf items join key keys last map merge next pop prev push reduce reverse serialize shift slice sort sortDesc splice sum take toArray values unique valid when unless unserialize"},{"location":"#creation","title":"Creation","text":""},{"location":"#__construct","title":"__construct()","text":"<p>An array of items can be passed directly into the constructor at the time of creation.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\n</code></pre>"},{"location":"#of","title":"of()","text":"<p>Creates a new Collection instance with any number of items in it.</p> <pre><code>$collection = Collection::of($foo, $bar, $baz);\n</code></pre>"},{"location":"#fromitems-callback","title":"from($items, $callback)","text":"<p>Creates a new Collection from an iterable class or object.</p> <pre><code>$items = ['foo', 'bar', 'baz'];\n$collection = Collection::from($items);\n</code></pre> <p>If <code>$items</code> is a class instance, it will make a collection from its public properties.</p> <pre><code>class Foo\n{\n    public $foo = 'foo';\n    public $bar = 'bar';\n    protected $baz = 'baz';\n}\n\n$foo = new Foo();\n$collection = Collection::from($foo);\n\n// $collection = ['foo', 'bar']\n</code></pre> <p>You can process each element in the collection by passing a callback as the second parameter.</p> <pre><code>$items = ['foo', 'bar', 'baz'];\n$collection = Collection::from($items, function($item) {\n    return strtoupper($item);\n});\n\n// $collection = ['FOO', 'BAR', 'BAZ']\n</code></pre>"},{"location":"#item-retrieval","title":"Item Retrieval","text":""},{"location":"#toarray","title":"toArray()","text":"<p>Returns the items in the collection as an array.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;toArray();\n// $collection = ['foo', 'bar', 'baz']\n</code></pre>"},{"location":"#items","title":"items()","text":"<p>Returns an ArrayIterator with all of the items in the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;items();\n// returns new ArrayIterator(['foo', 'bar', 'baz'])\n</code></pre>"},{"location":"#serialize","title":"serialize()","text":"<p>Returns a serialized version of the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;serialize();\n// returns 'a:3:{i:0;s:3:\"foo\";i:1;s:3:\"bar\";i:2;s:3:\"baz\";}'\n</code></pre>"},{"location":"#unserializeitems","title":"unserialize($items)","text":"<p>Creates a new collection out of a serialized array of items.</p> <pre><code>$serialized = 'a:3:{i:0;s:3:\"foo\";i:1;s:3:\"bar\";i:2;s:3:\"baz\";}';\n$collection = (new Collection())-&gt;unserialize($serialized);\n</code></pre>"},{"location":"#navigation","title":"Navigation","text":""},{"location":"#first","title":"first()","text":"<p>Returns the first item in the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;next();\n// returns 'foo'\n</code></pre>"},{"location":"#last","title":"last()","text":"<p>Returns the last item in the collection as it is currently sorted.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;last();\n// returns 'baz'\n</code></pre>"},{"location":"#next","title":"next()","text":"<p>Returns the next item in the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;next();\n// returns 'bar'\n</code></pre>"},{"location":"#prev","title":"prev()","text":"<p>Returns the previous item in the collection. If the cursor is currently at the first element, it will return the first element in the array.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;prev();\n// returns 'foo'\n</code></pre>"},{"location":"#at","title":"at()","text":"<p>Returns the item at the current offset. If the offset is negative it will return the item in the collection that is offset that many items from the end of the collection. The offset is zero-based.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;at(1);\n// returns 'bar'\n</code></pre>"},{"location":"#instance-methods","title":"Instance Methods","text":""},{"location":"#count","title":"count()","text":"<p>Returns the total number of items in the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;count();\n// returns 3\n</code></pre>"},{"location":"#averagekey","title":"average($key)","text":"<p>Returns the average of all items in the collection. If <code>$key</code> is provided, it will return the average of $key within each item.</p> <pre><code>$collection = new Collection([1, 2, 3, 4]);\nreturn $collection-&gt;average();\n// returns 2.5\n\n$collection = new Collection([\n    ['foo' =&gt; 10],\n    ['foo' =&gt; 20],\n    ['foo' =&gt; 30],\n    ['foo' =&gt; 40],\n]);\nreturn $collection-&gt;average('foo');\n// returns 25\n</code></pre>"},{"location":"#column","title":"column()","text":"<p>Returns a new collection from the original collection with single column of collection. </p> <pre><code>$collection = new Collection([\n    ['id' =&gt; 1, 'name' =&gt; 'John'],\n    ['id' =&gt; 2, 'name' =&gt; 'Carter'],\n    ['id' =&gt; 3, 'name' =&gt; 'Steve'],\n]);\nreturn $collection-&gt;column('name');\n// return ['John', 'Carter', 'Steve']\n</code></pre> <p>If an index is provided, the values from this column will be used as keys for the returned result in the collection.</p> <pre><code>$collection = new Collection([\n    (object) ['id' =&gt; 1, 'name' =&gt; 'John'],\n    (object) ['id' =&gt; 2, 'name' =&gt; 'Carter'],\n    (object) ['id' =&gt; 3, 'name' =&gt; 'Steve'],\n]);\nreturn $collection-&gt;column('name', 'id');\n// returns [1 =&gt; 'John', 2 =&gt; 'Carter', 3 =&gt; 'Steve']\n</code></pre>"},{"location":"#diff","title":"diff()","text":"<p>Returns a new collection from the original collection with only different items from passed in array or collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;diff(['foo', 'baz']);\n// returns [1 =&gt; 'bar']\n</code></pre> <p>You can also pass the column name as a <code>string</code> or multiple columns as <code>array</code> as a second parameter if you work with associative arrays or objects.</p> <pre><code>$collection = new Collection([\n    ['name' =&gt; 'John Doe 1', 'age' =&gt; 25],\n    ['name' =&gt; 'Jane Doe 2', 'age' =&gt; 30],\n    ['name' =&gt; 'John Doe 3', 'age' =&gt; 25],\n]);\nreturn $collection-&gt;diff([\n    ['name' =&gt; 'John Doe 1', 'age' =&gt; 25],\n    ['name' =&gt; 'John Doe 3', 'age' =&gt; 25],\n], 'name');\n// returns [1 =&gt; ['name' =&gt; 'Jane Doe 2', 'age' =&gt; 30]]\n</code></pre>"},{"location":"#each","title":"each()","text":"<p>Iterates over all items, passing each one into the callable.</p> <pre><code>$collection-&gt;each(function($item, $key) {\n    //\n});\n</code></pre> <p>You can stop iterating through the items by returning false from the callable.</p> <pre><code>$collection-&gt;each(function($item, $key) {\n    if (/* condition */) {\n        return false;\n    }\n});\n</code></pre>"},{"location":"#every","title":"every()","text":"<p>Used to determine if all items in the collection evaluate to true when passed to the given callable.</p> <pre><code>$collection = new Collection([1, 2, 3, 4]);\n$collection-&gt;every(function($item) {\n    return $item &gt; 2;\n});\n// returns false\n</code></pre> <p>If the $items array is empty, it will return <code>true</code>.</p>"},{"location":"#fillstart-end-value","title":"fill($start, $end, $value)","text":"<p>Fills all elements of the collection with a static value. The start value must be provided. If no end index is specified it will fill until the end of the collection.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;fill(1, 2, 9);\n// returns [1, 9, 9, 4, 5]\n</code></pre>"},{"location":"#filter","title":"filter()","text":"<p>Returns a new collection with only the items that satisfy the given callable by returning a boolean <code>true</code>.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5, 6]);\nreturn $collection-&gt;filter(function($item) {\n    return $item % 2 === 0;\n});\n// returns [2, 4, 6]\n</code></pre> <p>By default, this will only pass the value to the callback. You can pass <code>true</code> as the second parameter to pass both the value and the key to the callback.</p> <pre><code>$collection = new Collection(['foo' =&gt; 1, 'bar' =&gt; 2, 'baz' =&gt; 3]);\nreturn $collection-&gt;filter(function($value, $key) {\n    if ($key === 'bar') {\n        return $value;\n    }\n}, true);\n// returns 2\n</code></pre>"},{"location":"#find","title":"find()","text":"<p>Returns the first item in the collection that satisfies the give callback.</p> <pre><code>$collection = new Collection(['foo' =&gt; 1, 'bar' =&gt; 2, 'baz' =&gt; 2]);\nreturn $collection-&gt;find(function($item, $key) {\n    return $item &gt;= 2;\n});\n// returns 2\n</code></pre>"},{"location":"#findindex","title":"findIndex()","text":"<p>Like <code>find()</code> except it returns the index of the first item that satisfies the callback.</p> <pre><code>$collection = new Collection(['foo' =&gt; 1, 'bar' =&gt; 2, 'baz' =&gt; 3]);\nreturn $collection-&gt;findIndex(function($item, $key) {\n    return $item &gt; 2;\n});\n// returns 3\n</code></pre>"},{"location":"#flatten","title":"flatten()","text":"<p>Returns a new collection that is a flattened version of the original collection. If <code>$depth</code> is provided, it will only flatten that many levels. By default, it will flatten only 1 level deep.</p> <pre><code>$collection = new Collection([\n    'fruits' =&gt; ['apple', 'banana', 'orange'],\n    'vegetables' =&gt; ['carrot', 'tomato', 'cucumber'],\n]);\nreturn $collection-&gt;flatten();\n// returns ['apple', 'banana', 'orange', 'carrot', 'tomato', 'cucumber']\n</code></pre> <p>Keys for the flattened arrays are NOT preserved.</p> <pre><code>$collection = new Collection([\n    'a' =&gt; 1,\n    'b' =&gt; 2,\n    'c' =&gt; 3,\n    'd' =&gt; [\n        'e' =&gt; 4,\n        'f' =&gt; 5,\n        'g' =&gt; [\n            'h' =&gt; 6,\n            'i' =&gt; 7,\n        ],\n    ],\n]);\nreturn $collection-&gt;flatten();\n// returns [1, 2, 3, 4, 5, ['h' =&gt; 6, 'i' =&gt; 7]]\n</code></pre>"},{"location":"#groupby","title":"groupBy()","text":"<p>Returns a new collection with the items grouped by the given key.</p> <pre><code>$collection = new Collection([\n    ['name' =&gt; 'John', 'age' =&gt; 21],\n    ['name' =&gt; 'Jane', 'age' =&gt; 21],\n    ['name' =&gt; 'Bob', 'age' =&gt; 22],\n    ['name' =&gt; 'Mary', 'age' =&gt; 22],\n]);\nreturn $collection-&gt;groupBy('age');\n// returns [\n//     21 =&gt; [\n//         ['name' =&gt; 'John', 'age' =&gt; 21],\n//         ['name' =&gt; 'Jane', 'age' =&gt; 21],\n//     ],\n//     22 =&gt; [\n//         ['name' =&gt; 'Bob', 'age' =&gt; 22],\n//         ['name' =&gt; 'Mary', 'age' =&gt; 22],\n//     ],\n// ]\n</code></pre>"},{"location":"#includes","title":"includes()","text":"<p>Returns boolean <code>true</code> if the collection contains the given value, <code>false</code> otherwise.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;includes('bar');\n// returns true\n</code></pre>"},{"location":"#isempty","title":"isEmpty()","text":"<p>Returns boolean <code>true</code> if the collection is empty, <code>false</code> otherwise.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;isEmpty();\n// returns false\n\n$collection = new Collection();\nreturn $collection-&gt;isEmpty();\n// returns true\n</code></pre>"},{"location":"#indexof","title":"indexOf()","text":"<p>Returns the index of the given value, or <code>-1</code> if it is not found.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;indexOf('bar');\n// returns 1\n</code></pre>"},{"location":"#join","title":"join()","text":"<p>Return all values joined by a given string.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;join(', ');\n// returns 'foo, bar, baz'\n</code></pre> <p>If a second parameter is provided, it will be used as the last separator.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;join(', ', ' and ');\n// returns 'foo, bar and baz'\n</code></pre>"},{"location":"#key","title":"key()","text":"<p>Returns the key of the current item of the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;key();\n// returns 0\n\n$collection = new Collection(['foo' =&gt; 10, 'bar' =&gt; 15, 'baz' =&gt; 20]);\nreturn $collection-&gt;key();\n// returns 'foo'\n</code></pre>"},{"location":"#keys","title":"keys()","text":"<p>Returns a new collection with only the keys from the original collection.</p> <pre><code>$collection = new Collection(['foo' =&gt; 10, 'bar' =&gt; 15, 'baz' =&gt; 20]);\nreturn $collection-&gt;keys();\n// returns ['foo', 'bar', 'baz']\n</code></pre>"},{"location":"#map","title":"map()","text":"<p>Returns a new collection with the results of the callback applied to each item.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;map(function($item) {\n    return $item * 2;\n});\n// returns [2, 4, 6, 8, 10]\n</code></pre>"},{"location":"#merge","title":"merge()","text":"<p>Merges the given array or collection with the current collection. This uses the <code>array_merge()</code> function, so any duplicate keys will be overwritten by the new values.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;merge(['a', 'b', 'c']);\n// returns ['foo', 'bar', 'baz', 'a', 'b', 'c']\n</code></pre>"},{"location":"#pop","title":"pop()","text":"<p>Removes and returns the last item in the collection, shortening the items by 1. If no items are present <code>null</code> is returned.</p> <p>NOTE: This method is NOT immutable and affects the original collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;pop();\n// returns 'baz'\n// $collection = ['foo', 'bar']\n</code></pre>"},{"location":"#push","title":"push()","text":"<p>Adds one or more items to the end of the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;push('a', 'b', 'c');\n// returns ['foo', 'bar', 'baz', 'a', 'b', 'c']\n</code></pre>"},{"location":"#reduce","title":"reduce()","text":"<p>Returns the value created when the callback is applied to each item in the collection, passing the result into the next callback, and so on.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;reduce(function($carry, $item) {\n    return $carry + $item;\n});\n// returns 15\n</code></pre>"},{"location":"#reverse","title":"reverse()","text":"<p>Returns a new collection with the items in reverse order.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;reverse();\n// returns ['baz', 'bar', 'foo']\n</code></pre>"},{"location":"#shift","title":"shift()","text":"<p>Removes and returns the first item in the collection, shortening the items by 1. If no items are present <code>null</code> is returned.</p> <p>NOTE: This method is NOT immutable and affects the original collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;shift();\n// returns 'foo'\n// $collection = ['bar', 'baz']\n</code></pre>"},{"location":"#slice","title":"slice()","text":"<p>Returns a new collection with only the items between and including the given start and end indexes. If no end index is provided, it will return all items from the start index to the end of the collection.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz', 'a', 'b', 'c']);\nreturn $collection-&gt;slice(2, 4);\n// returns ['baz', 'a', 'b']\n</code></pre>"},{"location":"#sort","title":"sort()","text":"<p>Returns a new collection with the items sorted in ascending order by the given callback. If no callback is provided, it will sort the items alphabetically.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;sort();\n// returns ['bar', 'baz', 'foo']\n\n$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;sort(function($a, $b) {\n    return $a &lt;=&gt; $b;\n});\n// returns ['bar', 'baz', 'foo']\n</code></pre>"},{"location":"#sortdesc","title":"sortDesc()","text":"<p>Returns a new collection with the items sorted in descending order by the given callback. If no callback is provided, it will attempt sort the items alphabetically.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;sortDesc();\n// returns ['foo', 'baz', 'bar']\n\n$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;sortDesc(function($a, $b) {\n    return $a &lt;=&gt; $b;\n});\n// returns ['foo', 'baz', 'bar']\n</code></pre>"},{"location":"#splice","title":"splice()","text":"<p>Returns a new collection with the portion of the items of the collection either removed or replaced with a new item(s).</p> <p>NOTE: This method is NOT idempotent and affects the original collection.</p> <pre><code>$collection = new Collection(['red', 'green', 'yellow', 'blue']);\n$new = $collection-&gt;splice(2);\n// $new = ['yellow', 'blue']\n// $collection = ['red', 'green']\n</code></pre> <p>You can also replace the removed items with new items. The second parameter is the number of items to remove. The third parameter is an array of items to insert into the original collection.</p> <pre><code>$collection = new Collection(['red', 'green', 'yellow', 'blue']);\n$new = $collection-&gt;splice(2, 1, 'orange');\n// $new = ['yellow']\n// $collection = ['red', 'green', 'orange', 'blue']\n</code></pre>"},{"location":"#sumkey","title":"sum($key)","text":"<p>Returns the sum of all items in the collection. If <code>$key</code> is provided, it will return the sum of $key within each item.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;sum();\n// returns 15\n</code></pre> <pre><code>$collection = new Collection([\n    ['foo' =&gt; 10],\n    ['foo' =&gt; 20],\n    ['foo' =&gt; 30],\n    ['foo' =&gt; 40],\n]);\nreturn $collection-&gt;sum('foo');\n// returns 100\n</code></pre>"},{"location":"#take","title":"take()","text":"<p>Returns a collection with the first or last items based on <code>$limit</code>.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;take(2);\n// returns [1, 2]\n</code></pre> <p>You can also use negative number, to get items from the end of collection.</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;take(-2);\n// returns [4, 5]\n</code></pre> <p>To preverse keys, you can use the second parameter:</p> <pre><code>$collection = new Collection([1, 2, 3, 4, 5]);\nreturn $collection-&gt;take(-2, true);\n// returns [3 =&gt; 4, 4 =&gt; 5]\n</code></pre>"},{"location":"#unique","title":"unique()","text":"<p>Returns a new collection with only unique items from the original collection.</p> <pre><code>$collection = new Collection([1, 2, 2, 3, 1, 5, 1, 3]);\nreturn $collection-&gt;unique();\n// returns [0 =&gt; 1, 1 =&gt; 2, 3 =&gt; 3, 5 =&gt; 5]\n</code></pre> <p>You can also pass the column name as a <code>string</code> or multiple columns as <code>array</code> as parameter if you work with associative arrays or objects.</p> <pre><code>$collection = new Collection([\n    ['id' =&gt; 1, 'name' =&gt; 'John'],\n    ['id' =&gt; 2, 'name' =&gt; 'Jane'],\n    ['id' =&gt; 1, 'name' =&gt; 'Jim'],\n    ['id' =&gt; 3, 'name' =&gt; 'Joe'],\n]);\nreturn $collection-&gt;unique('id');\n// returns [\n//    0 =&gt; ['id' =&gt; 1, 'name' =&gt; 'John'],\n//    1 =&gt; ['id' =&gt; 2, 'name' =&gt; 'Jane'],\n//    3 =&gt; ['id' =&gt; 3, 'name' =&gt; 'Joe'],\n//]\n</code></pre>"},{"location":"#values","title":"values()","text":"<p>Returns a new collection with only the values from the original collection.</p> <pre><code>$collection = new Collection([\n    'foo' =&gt; 10,\n    'bar' =&gt; 15,\n    'baz' =&gt; 20\n]);\nreturn $collection-&gt;values();\n// returns [10, 15, 20]\n</code></pre>"},{"location":"#valid","title":"valid()","text":"<p>Returns boolean <code>true</code> if the current item is valid (has a key), <code>false</code> otherwise.</p> <pre><code>$collection = new Collection(['foo', 'bar', 'baz']);\nreturn $collection-&gt;valid();\n// returns true\n</code></pre>"},{"location":"#whencallback","title":"when($callback)","text":"<p>Returns the result of the callback if the collection is not empty. If the collection is empty, it will return the collection.</p> <pre><code>$collection = new Collection([\n    'a' =&gt; 1,\n    'b' =&gt; 2,\n    'c' =&gt; 3,\n    'd' =&gt; 4,\n    'e' =&gt; 5,\n]);\n$new = $collection-&gt;when(function ($item) {\n    return $item % 2 === 0;\n});\n// returns ['b' =&gt; 2, 'd' =&gt; 4]\n</code></pre>"},{"location":"#unlesscallback","title":"unless($callback)","text":"<p>Returns a new collection that contains all items where the callback evaluates to <code>false</code>.</p> <pre><code>$collection = new Collection([\n    'a' =&gt; 1,\n    'b' =&gt; 2,\n    'c' =&gt; 3,\n    'd' =&gt; 4,\n    'e' =&gt; 5,\n]);\n$new = $collection-&gt;unless(function ($item) {\n    return $item % 2 === 0;\n});\n// returns ['a' =&gt; 1, 'c' =&gt; 3, 'e' =&gt; 5]\n</code></pre>"}]}